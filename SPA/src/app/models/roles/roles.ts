import { Role } from "./role";

export class Roles {
  public static admin: Role = {
    name: "Admin"
  };
  public static user: Role = {
    name: "User"
  };
  public static driver: Role = {
    name: "Driver"
  };
}
